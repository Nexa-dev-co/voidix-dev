# dependencies
# Un-anchored: the app lives at apps/web, so these must match at any depth.
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/

# production
build/

# turbo / workspace build caches
.turbo/

# misc
.DS_Store
*.pem

# local tooling caches
.impeccable/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

# Raw, pre-compression source models (optimized copies live in apps/web/public/models)
models-src/

# Scratch space for raw model sources being inspected (see models-src for the kept copies)
temp/
