# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env
.env.example
# typescript
*.tsbuildinfo
next-env.d.ts

# Raw, pre-compression source models (optimized copies live in public/models)
models-src/

# Raw, lossless source textures (compressed copies live in public/textures).
# Unlike models-src these WERE once committed, so if this directory is lost the originals are still
# in git history — see scripts/optimizeTextures.mjs.
textures-src/

# Scratch space for raw model sources being inspected (see models-src for the kept copies)
/temp
