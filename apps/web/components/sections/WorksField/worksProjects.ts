// The works projects moved to @voidix/content — see the note in ServicesDeck/deckServices.ts for
// why. This file stays as the path the field and its tuner import from.

export { WORKS_PROJECTS } from '@voidix/content';

export type { ProjectRock, WorksProject } from '@voidix/content';
