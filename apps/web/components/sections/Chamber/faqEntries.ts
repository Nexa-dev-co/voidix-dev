// The FAQ copy moved to @voidix/content — see the note in ServicesDeck/deckServices.ts for why.
// This file stays as the path the chamber hologram imports from.

export { FAQ_ENTRIES } from '@voidix/content';

export type { FaqEntry } from '@voidix/content';
